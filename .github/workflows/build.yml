name: Build

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  packages: read

jobs:
  build:
    uses: Mazaadak/.github/.github/workflows/java-build-package-push.yml@main
    with:
      java-version: '21'
      skip-tests: true
    secrets: inherit